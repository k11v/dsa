fn main() {
    // one machine per line
    // [...] - single indicator light diagram
    // indicator lights - initially off
    // (...) (...) - one or more button wiring schematics
    // buttons toggle linked indicator lights, 0-based
    // {...} - joltage requirements
    // ignored in part-1
    // fewest total presses
}
